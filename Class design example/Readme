Her, no ane name are instance variables and these get initialized when we create an object for the Employee. If we want to call display() method which is an instance method, we must create an object for the class. 